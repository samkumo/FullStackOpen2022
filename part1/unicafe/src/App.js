import { useState } from "react";

const App = () => {
  // save clicks of each button to it's own state
  const [good, setGood] = useState(0)
  const [neuttral, setNeutral] = useState(0)
  const [bad, setBad] = useState(0)

  return (
    <div>

    </div>
  )
}

export default App;
